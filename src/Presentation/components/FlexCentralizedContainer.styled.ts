import styled from 'styled-components/native';
import {Colors} from '../assets/colors/colors';

export const FlexCentralizedContainer = styled.View`
  flex: 1;
  justify-content: center;
  align-items: center;
  background-color: ${Colors.background};
`;
